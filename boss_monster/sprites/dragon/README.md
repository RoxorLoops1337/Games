# Dragon boss — idle animation

4-frame idle that plays as a PING-PONG loop (forward → reverse):
frame 0 → 1 → 2 → 3 → 2 → 1 → 0 → 1 → … (so it eases back and forth, no jump).

Upload via GitHub's "Add file → Upload files" (not as a chat image) so
transparency is preserved. Export WITHOUT a baked shadow.

Either format is fine — I'll normalise the names:
- 4 separate PNGs (any names; I'll rename to dragon_idle_0.png … dragon_idle_3.png), OR
- one sheet with the 4 frames in a row (tell me the frame size / layout and I'll slice it).

All 4 frames should be the same size and aligned (same canvas) so the loop doesn't jitter.
