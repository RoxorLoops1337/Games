{
  "name": "games-crypto",
  "version": "0.1.0",
  "private": true,
  "description": "Fan rewards: ERC-20 token + ERC-721 NFT collection on Base.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network baseSepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network base",
    "reward:testnet": "hardhat run scripts/reward.js --network baseSepolia",
    "reward:mainnet": "hardhat run scripts/reward.js --network base",
    "verify:testnet": "hardhat run scripts/verify.js --network baseSepolia",
    "verify:mainnet": "hardhat run scripts/verify.js --network base"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "5.0.2",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.5",
    "solc": "0.8.24"
  }
}
